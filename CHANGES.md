# 简历工作台 UI 更新

## 双栏版面与新增模板

- 双栏侧栏改为贯穿纸张左侧的版面分区，取消内嵌卡片背景。姓名和联系方式归入侧栏；保留内容就地编辑、栏宽、栏间距及板块分配。
- 侧栏宽度以整张纸为基准，背景随较长一栏延伸。PDF 通过正文底色与上下页边距块拼接，保证多页连续侧栏，同时保留每页的文字留白。
- 新增时间轴、名片、分节三款模板，总数为九款；缩略图对应真实结构，数量由模板目录自动计算。
- 模板切换仅改变模板字段，现有内容、字体、配色和自定义参数保持。新模板纳入备份、链接分享和输入校验。
- 新增纸边对齐、侧栏全高、极限栏宽/间距、就地编辑、新模板链接还原、四页双栏分页等回归检查。
- 验证：28 项浏览器回归测试通过；三款新模板默认内容均导出为单页 A4，四页双栏样本的两栏记录、每页文字留白与通栏背景已检查。

## 控件细节与中文字体修订

- 用圆角浮层替换原生颜色选择器，统一色块、二维选色区域、色相滑块、HEX/RGB 输入与屏幕取色按钮；实时保存配色并纳入备份、分享快照。
- 全部静态和动态下拉菜单改用同一组件，保留方向键、Home/End、回车、Escape、Tab 和字母查找，浮层自动避让屏幕边缘，适配深色与减少动态效果。
- 字体主选项改成支持中文的 Noto Sans SC、鸿蒙黑体、未来荧黑、未来荧黑紧凑版与系统黑体。旧版西文字体组合继续兼容，读取旧组合时才显示中文搭配；新建方案采用内置中文字体。
- 新增未来荧黑常规/紧凑两种实际字形，每种内置常规和粗体，附原始 OFL 许可，按当前字符分片加载。
- 分享窗口关闭按钮改用几何居中的 SVG，链接框由外层裁切圆角，去掉原生缩放角和粗滚动条。板块显隐勾选框同步改成圆角。
- 新增浏览器测试验证颜色输入/拖动/存储、下拉键盘操作、中文实际渲染差异、旧字体分享兼容、关闭图标中心、窄屏和深色浮层。

## 全面界面与排版升级

- 删除旧编辑器样式层，`style.css` 专注简历模板，`workspace.css` 统一操作界面。移除 Logo 和图形 favicon，使用文字名称。
- 浅色与深色采用统一语义色；编辑区减少重复描边，放大字段字体，长地址与职位改为完整行。工具栏保持可见，侧栏、通知、JSON、AI 提示词、分享和删除确认使用同一控件体系。
- 移除旧缩放气泡；滚轮、缩放按钮和复位共用同一逻辑。滑块加入填充与按压动效、数值精调、按帧更新；折叠可中断，支持减少动态效果。
- 新增留白、目录、映色三款模板，优化经典、双栏、居中三款。修复技能标签窄列换行、长条目换行和工作经历误标为实习经历的问题。
- 增加八组配色、八种无衬线选择及保留的等宽字体；新增 Manrope、DM Sans、Noto Sans SC 本地资源与许可证。中文分片按需加载。修复空缺字体引用，明确鸿蒙字体常规字重与合成粗体限制。
- 字体切换不再重置密度。新建简历使用 10.5px 正文，已保存方案保留原参数；紧凑、均衡、舒展预设由用户主动选择。
- 新增布局页：页头对齐、标题样式、姓名字号、字距、图标显隐、板块顺序/隐藏、标题改名、双栏比例/间隔/板块分配。所有参数随 A/B 方案、备份及链接保存。
- 修复滚动后打印可能裁掉页头的问题；每页使用设定页边距，导出前等待字体。长文档允许跨页，尽量保持单条经历完整。

## URL 分享简历

- 新增侧栏和预览工具栏的分享入口：简历内容与当前方案排版生成自包含的 gzip + Base64URL 快照，接收方打开链接直接还原。
- 分享数据保存在 URL fragment 中，无后端和运行时依赖；支持 Unicode、加粗、自定义板块及三种模板的全部设置。
- 分享会话的内容与 A/B 排版写入内存，接收方的本机简历不被覆盖，提供返回本机入口；刷新读取原始快照，修改后可重新分享或导出备份。
- 为损坏/未知版本/超大链接提供校验和错误恢复；剪贴板受限时选中链接供手动复制；本机地址和文件模式显示明确说明。
- 新增浏览器回归测试覆盖三模板还原、共享会话各编辑路径、Unicode 与 HTML 安全、压缩/未压缩格式、损坏与过大数据、分享窗口以及手机预览。

## 悬浮岛修订

- 移除通栏顶部和重复的预览说明，将品牌与主题按钮收进左侧面板，右侧合并为 50px 高的胶囊工具栏。1440px 桌面宽度下纸张顶部从约 200px 提升到 82px。
- 左侧面板四周留空，使用 28px 圆角；表单、选项卡和底部操作区采用独立圆角层级。仅小面积工具栏使用背景模糊。
- 重新绘制 E/C 弧线与 V 形勾组合的 SVG Logo，同步更新页面图标。
- 修复联系方式只清空文字而保留图标的问题。GitHub、作品集等留空或只有空格时整项隐藏，重新填写后同步恢复链接；就地清空也移除图标。
- 新增覆盖三种模板、打印样式、空值/空格/重新填写及悬浮布局的回归测试。

采用浅色工作台、分组表单、圆角分段控件和克制的材质层级。保留深色主题，手机宽度下通过“编辑内容 / 简历预览”切换操作区。预览提供缩放与适合宽度按钮，放大后可以滚动查看整页。

原有三种模板、A/B 独立排版与共享内容、列表增删排序、自定义板块、就地编辑和加粗、JSON 编辑与备份、AI 提示词、字体/配色/密度调整、打印 PDF 均保留。

修复：

- JSON/API 导入先完整校验再替换数据；缺少 settings 时保留当前排版，缺少可选列表时补齐。
- 表单属性和简历预览按上下文转义，保留受限的加粗标记；粘贴内容过滤事件属性与脚本。
- API 的 state 始终返回当前方案；阻止危险对象路径写入。
- 字体间距预设使用正确的 secSpacing 字段；导入后更新模板、字体和配色高亮。
- 教育背景的学位和专业可以分别编辑，避免重复拼接；长自定义标题的副标题独立成行。
- 就地编辑失焦不再立刻销毁下一处将要编辑的节点。
- 删除无效的 CSS 结束括号，明确 A4 打印和移动端打印规则。

性能：

- 保持原生 DOM 实现，无运行时框架或动效依赖。
- 图标与现有西文字体改为本地 WOFF2，浏览器按使用的字体和字重加载；默认页面不请求外站。
- 连续输入合并为 250ms 后保存，切换方案、隐藏页面时立即保存；停止重复写入旧格式的大缓存。
- 移除简历元素常驻的 will-change，拖动侧栏时只在结束后保存宽度；支持减少动画和减少透明效果偏好。

验证：Windows Edge 下 5 个浏览器集成测试通过，覆盖编辑、保存与重新加载、A/B/模板切换、JSON 和文件导入导出、列表增删排序、恶意 HTML、手机布局、键盘操作、缩放、离线资源及打印。三种模板的默认示例均导出为 1 页 A4 PDF。桌面默认页面本地资源解码体积约 1.0 MB；这不是 CPU/内存基准测试。

测试截图和 PDF 在本地 `artifacts/`（不提交）。Safari、Firefox 和实体手机尚未实机测试。
